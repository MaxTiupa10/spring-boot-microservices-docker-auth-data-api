# 1. Беремо базовий образ з Java 17 (або 21, залежно від вашого pom.xml)
FROM amazoncorretto:17

# 2. Робимо робочу папку всередині контейнера
WORKDIR /app

# 3. Копіюємо JAR файл, який згенерував Maven, всередину контейнера
# (Зверніть увагу: це спрацює, тільки якщо ви перед цим зробили mvn package)
COPY target/*.jar app.jar

# 4. Команда для запуску
ENTRYPOINT ["java", "-jar", "app.jar"]